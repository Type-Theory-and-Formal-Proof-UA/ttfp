#import "/book.typ": *

== Додаткова література

Формальний підхід до натуральних чисел сягає 1889 року (G. Peano; переклад
оригінальної статті Peano можна знайти у van Heijenoort, 1967). Цей опис є
аксіоматичним, тож він описує функції не як програми чи алгоритми, а як символи,
що задовольняють певні рівняння. На початку двадцятого століття постали питання
обчислюваності та розв'язності, коли D. Hilbert запитав, чи існує процедура, яка
механічно вирішує, чи формула істинна. Пізніше це питання було далі уточнено
розрізненням між питанням, чи формула $A$ істинна (в усіх моделях або в деякій
конкретній моделі), і питанням, чи формула $A$ вивідна в певній формальній
системі. A.M. Turing (Turing, 1936) і A. Church (Church, 1936b) показали, що ці
питання нерозв'язні: не існує машини (комп'ютерної програми), яка на вході $A$
вирішувала б, чи вона істинна, відповідно чи вона вивідна (якщо не обмежитися
логікою обмеженої виразності, як-от числення висловлювань). Щодо «істинності»
ситуація навіть тонша: знаменита теорема про неповноту K.F. Gödel (Gödel, 1932)
показує, що не існує такої системи виведення, яка могла б охопити всі формули,
істинні в $NN$.

Це також дало початок характеризації обчислюваних функцій — спершу в Тюринга
(Turing, 1936) як класу функцій, які можна обчислити за допомогою (того, що
згодом стало відоме як) машини Тюринга. Після Тюринга було означено різні
«моделі» обчислення, і всі вони мають чудову властивість: вони охоплюють той
самий клас обчислюваних функцій. Це спонукало Тюринга й Черча сформулювати тезу,
що будь-яку функцію, яку можна обчислити механічним пристроєм, можна обчислити й
машиною Тюринга. Див., напр., Lewis & Papadimitriou (1981) або Sudkamp (2006) як
вступ.

Клас обчислюваних функцій можна означити різними способами. Популярний спосіб,
який абстрагується від «моделі машини», — означити його як клас
$mu$-рекурсивних функцій. Це клас функцій, який містить нульову функцію та
функцію наступника і замкнений відносно операцій композиції функцій, примітивної
рекурсії та мінімізації. Знову ж таки, див. Sudkamp (2006). Схема примітивної
рекурсії по суті стверджує, що функція $f$, яка має значення для входу 0, а для
входу $n + 1$ використовує лише свій результат на вході $n$ (тобто $f(n)$), є
обчислюваною.

Наш текст не про обчислюваність функцій, а лише про коректну означеність. Ми
вживаємо рекурсивні означення як механізм для означення функції, напр. додавання.
Щоб переконатися, що таке рекурсивне означення справді означає щось осмислене, ми
мусимо забезпечити, щоб рекурсивні виклики в означеннях мали «менші» аргументи.
Тому ми вживаємо простий окремий випадок схеми примітивної рекурсії, щоб мати
належний механізм для означення функцій рекурсією. Наша теорема про рекурсію для
$ZZ$ (теорема 14.4.3) стверджує, що окремий випадок схеми примітивної рекурсії дає
коректно означену функцію. У тексті підрозділу 14.4 ми докладно обґрунтували це на
прикладі додавання.

У справжній математиці, зокрема в теорії чисел, частіше працюють із цілими
числами, ніж із натуральними. Однак натуральні числа — це гарна індуктивно
означена множина, що дає початок доведенням індукцією та означенням обґрунтованою
рекурсією. У цьому розділі ми використовуємо працю A. Margaris (Margaris, 1961),
щоб аксіоматично ввести цілі числа, принцип індукції над цілими числами, а також
схему означення функцій рекурсією над цілими числами. Насправді підхід Margaris за
стилем дуже близький до оригінального підходу Пеано для натуральних чисел.

Можлива альтернатива — означити множину цілих чисел $ZZ$ як фактор-множину
множини пар натуральних чисел $NN times NN$. Цей підхід можна знайти, напр., у van
Dalen et al., 1978, розділ 11. Повторимо істотні моменти: відношення
еквівалентності $tilde$ над $NN times NN$ означують так: $(k, l) tilde (m, n)$,
якщо $k + n = l + m$. (Інтуїтивно: різниця між $k$ і $l$ така сама, як між $m$ і
$n$.) Тепер $ZZ$ означують як множину класів еквівалентності $NN times NN$ за
відношенням $tilde$. В абстрактній математиці це цілком працює, але якщо це справді
потрібно використати в теорії типів, то це громіздко. По-перше, для цього потрібне
поняття «клас еквівалентності». Його можна ввести (див. підрозділ 13.4), але ми
воліємо не робити цього, якщо воно не потрібне. Другий недолік полягає в тому, що
всі функції доводиться означувати «за модулем відношення еквівалентності»
$tilde$: функція з $NN times NN$ в $NN times NN$ дає початок функції з $ZZ$ в
$ZZ$ лише тоді, коли вона узгоджується з $tilde$, а це властивість, яку треба
перевіряти щоразу.

== Вправи

#exercise[
  #enum(
    numbering: "(a)",
    [(Див. підрозділ 14.2.) Доведіть у #ld: $forall x : ZZ . (not (x epsilon NN) arrow.r.double not (p x epsilon NN))$.],
    [Наведіть неформальне доведення такого твердження: якщо
      $x = p(dots.c (p 0) dots.c)$, з щонайменше одним входженням $p$ у правій
      частині, то $not (x epsilon NN)$.],
  )

  Як і у вправах до попереднього розділу, у цих вправах ви можете обмежитися лише
  *згадуванням* логічних правил, які ви використовуєте в #(ld)-виведеннях (пор.
  підрозділ 13.2). Це стосується також застосувань правил рівності, як-от eq-subs,
  eq-cong 1 і eq-subs. Доводячи нумеровану лему з тексту книжки, ви можете
  покликатися на раніші леми, але не на пізніші.
]

#exercise[
  (Див. рисунок 14.5.) Виведіть терми доведення, що відповідають:

  #enum(
    numbering: "(a)",
    [zero-prop,],
    [clos-prop.],
  )

  (Ви можете додати проміжні судження.)
]

#exercise[
  (Див. рисунок 14.5.) Доведіть у #ld, що $NN$ — найменша підмножина, яка
  задовольняє nat-cond, знайшовши мешканця

  #align(center)[
    $Pi Q : ZZ -> ast_p . ("nat-cond"(Q) arrow.r.double NN subset.eq Q).$
  ]
]

#exercise[
  Якщо $ZZ$ формалізовано, як у підрозділі 14.2, включно з ax-int 3, то всі
  моделі $ZZ$ є нескінченними. Наведіть неформальне доведення цього.
]

#exercise[
  Наведіть #(ld)-доведення леми 14.3.1.
]

#exercise[
  Наведіть #(ld)-доведення: $forall x : ZZ . ("neg"(p x) or "pos"(s x))$.
]

#exercise[
  Наведіть #(ld)-доведення всіх частин леми 14.3.2.
]

#exercise[
  Наведіть #(ld)-доведення леми 14.3.3 (a).
]

#exercise[
  (Див. зауваження 14.4.2.) Відношення $R : S -> S -> ast_p$ називається
  *обґрунтованим*, якщо не існує нескінченної «спадної» послідовності
  $x_0, x_1, x_2, dots.c$ елементів $S$ такої, що $x_(i+1) R x_i$ для всіх $i$.

  #enum(
    numbering: "(a)",
    [Наведіть неформальний опис обґрунтованого відношення на $ZZ$, яке
      відповідає схемі рекурсії з теореми 14.4.3.],
    [Поясніть, чому відношення, означене в пункті (a), перестає бути
      обґрунтованим, якщо видалити умови «$"pos"(s x)$» і «$"neg"(p x)$» зі схеми
      рекурсії в теоремі 14.4.3.],
  )
]

#exercise[
  #enum(
    numbering: "(a)",
    [Нехай $A$ — тип, $f : A -> A$ — бієкція і $g : ZZ -> A$. Розгляньте такі
      твердження:

      #enum(
        numbering: "(1)",
        [$forall x : ZZ . [("pos"(s x) arrow.r.double g(s x) =_(A) f (g x)) and ("neg"(p x) arrow.r.double g(p x) =_(A) f^(-1) (g x))]$,],
        [$forall x : ZZ . (g(s x) =_(A) f (g x))$.],
      )

      Доведіть (1)$arrow.l.r.double$(2) (вказівка для (1)$arrow.r.double$(2):
      див. вправу 14.6, лему 14.3.2 (c) і вправу 13.14).],
    [Доведіть неформально, що теорема 14.4.5 є наслідком теореми про рекурсію для
      $ZZ$, тобто теореми 14.4.3.],
  )
]

#exercise[
  Заповніть прогалини в рядках (2) до (4) рисунка 14.12.
]

#exercise[
  Наведіть неформальні доведення таких властивостей додавання:

  #enum(
    numbering: "(a)",
    [Комутативності (лема 14.6.2) (вказівка: візьміть $x$ фіксованим і застосуйте
      симетричну індукцію за $y$; скористайтеся лемою 14.6.1),],
    [Асоціативності (лема 14.6.4) (вказівка: візьміть $x$ і $y$ фіксованими та
      застосуйте симетричну індукцію за $z$),],
    [правого закону скорочення (лема 14.6.5 (a)).],
  )
]

#exercise[
  Наведіть доведення в #(ld)-форматі частини II леми 14.7.2.
]

#exercise[
  Подайте доведення леми 14.8.6 (b) у #(ld)-форматі.
]

#exercise[
  Наведіть неформальні доведення:

  #enum(
    numbering: "(a)",
    [леми 14.8.8 (a) і (b),],
    [лівого закону скорочення для віднімання (лема 14.8.9 (b)) (вказівка:
      почніть із двократного застосування леми 14.8.2).],
  )
]

#exercise[
  Наведіть неформальні доведення леми 14.8.10 (a) і (c).
]

#exercise[
  Доведіть у #ld, що для кожного $x$ з $ZZ$ існує точно одне $y$ з $ZZ$ таке, що
  $x + y = 0$.
]

#exercise[
  (Див. зауваження 14.2.1.) Доведіть у #ld, що з ax-int 2 випливає такий варіант
  індукції для $ZZ$, із довільним $P l$ замість $P 0$:

  #align(center)[
    $((exists l : ZZ . P l) and forall x : ZZ . (P x arrow.r.double (P(s x) and P(p x)))) arrow.r.double forall x : ZZ . P x.$
  ]
]

#exercise[
  Наведіть неформальні доведення та нариси #(ld)-версій для:

  #enum(
    numbering: "(a)",
    [трьох частин леми 14.9.2,],
    [двох частин леми 14.9.3,],
    [двох частин леми 14.9.7.],
  )
]

#exercise[
  Наведіть нариси доведень у #(ld)-стилі таких лем:

  #enum(
    numbering: "(a)",
    [$forall x, y : ZZ . (x - (-y) = x + y)$,],
    [$forall x, y : ZZ . (-(x - y) = (-x) + y)$,],
    [$forall x, y : ZZ . (x - y = -(y - x))$.],
  )
]

#exercise[
  (Див. зауваження 14.4.4.)

  #enum(
    numbering: "(a)",
    [Означте функцію абсолютної величини $"abs" : ZZ -> ZZ$ за допомогою теореми
      про рекурсію для $ZZ$.],
    [Доведіть у #ld, що $forall x : ZZ . (x epsilon NN arrow.r.double "abs" x = x)$.],
    [Доведіть у #ld, що $forall x : ZZ . (x epsilon NN arrow.r.double "abs"(-x) = x)$.],
  )
]

#exercise[
  Наведіть нарис доведення такої леми:

  #align(center)[
    $forall x, y : ZZ . ("abs"(x - y) = "abs"(y - x)).$
  ]

  (Вказівка: див. вправу 14.20 (c) і лему 14.8.11.)
]

#exercise[
  Доведіть у #ld:

  #enum(
    numbering: "(a)",
    [$forall x : ZZ . (x < s x)$,],
    [$forall x : ZZ . (x > p x)$.],
  )
]

#exercise[
  Доведіть у #ld:

  #enum(
    numbering: "(a)",
    [$forall x, y : ZZ . (x < y arrow.l.r.double s x < s y)$,],
    [$forall x, y : ZZ . (x < y arrow.l.r.double p x < p y)$.],
  )
]

#exercise[
  Наведіть нариси доведень у #(ld)-стилі таких лем:

  #enum(
    numbering: "(a)",
    [$forall x, y : ZZ . ((x epsilon NN and x lt.eq y) arrow.r.double y epsilon NN)$,],
    [$forall x, y : ZZ . (("neg"(y) and x lt.eq y) arrow.r.double "neg"(x))$.],
  )
]

#exercise[
  Наведіть нариси доведень леми 14.10.1 (d) і (e).
]

#exercise[
  Наведіть нариси доведень леми 14.10.2 (a) і (b).
]

#exercise[
  Перетворіть неформальні доведення леми 14.10.3 (a) і (b) на #(ld)-доведення.
]

#exercise[
  Наведіть нариси доведень у #(ld)-стилі таких лем:

  #enum(
    numbering: "(a)",
    [$forall x, y : ZZ . ((x lt.eq y and y lt.eq x) arrow.r.double x = y)$,],
    [$forall x, y : ZZ . (x < y arrow.r.double s x lt.eq y)$.],
  )
]

#exercise[
  На початку підрозділу 14.11 ми ствердили, що з рекурсивного рівняння

  #block(inset: (left: 1.5em))[
    (ii) $m dot s(n) = (m dot n) + m$
  ]

  випливає рівняння

  #block(inset: (left: 1.5em))[
    (iii) $m dot p(n) = (m dot n) - m$.
  ]

  Покажіть це.
]

#exercise[
  Наведіть неформальні доведення та нариси #(ld)-версій для трьох частин леми
  14.11.1.
]

#exercise[
  Наведіть неформальні доведення двох частин леми 14.11.2.
]

#exercise[
  Наведіть неформальні доведення двох частин леми 14.11.3.
]

#exercise[
  Наведіть нариси #(ld)-доведень для:

  #enum(
    numbering: "(a)",
    [$1 dot 1 = 1$,],
    [$2 dot 2 = 4$,],
    [$(-1) dot (-1) = 1$.],
  )
]

#exercise[
  Наведіть виведення в #ld таких лем:

  #enum(
    numbering: "(a)",
    [$forall x : ZZ . (x dot 1 = x)$,],
    [$forall x : ZZ . (x dot (-1) = -x)$.],
  )
]

#exercise[
  Наведіть неформальне доведення та нарис доведення в #ld для твердження, що $NN$
  замкнена відносно множення (лема 14.11.5 (a)).
]

#exercise[
  Наведіть неформальне доведення леми 14.11.6 (вказівка: скористайтеся лемами
  14.10.2 (c) і 14.11.5).
]

#exercise[
  Наведіть неформальне доведення леми 14.11.7.
]

#exercise[
  Наведіть неформальні доведення таких лем:

  #enum(
    numbering: "(a)",
    [$forall x, y, z : ZZ . ((x lt.eq y and z epsilon NN) arrow.r.double x dot z lt.eq y dot z)$,],
    [$forall x, y, z : ZZ . ((x dot z lt.eq y dot z and "pos"(z)) arrow.r.double x lt.eq y)$,],
    [$forall x, y, z : ZZ . ((x dot z lt.eq y dot z and "neg"(z)) arrow.r.double x gt.eq y)$.],
  )
]

#exercise[
  Наведіть неформальні доведення:

  #enum(
    numbering: "(a)",
    [леми 14.12.1 (a) і (c),],
    [леми 14.12.2 (a).],
  )
]

#exercise[
  Наведіть неформальні доведення таких лем:

  #enum(
    numbering: "(a)",
    [$forall x : ZZ . (x epsilon NN arrow.r.double (x = 0 or x gt.eq 1))$,],
    [$forall x, y : ZZ . (x epsilon NN arrow.r.double (x dot y = 1 arrow.r.double x = 1))$
      (вказівка: скористайтеся пунктом (a)).],
  )
]

#exercise[
  Наведіть неформальне доведення леми 14.12.3 (c).

  (Вказівка: скористайтеся вправою 14.41 (b).)
]

#exercise[
  Доведіть у #ld таке:

  #align(center)[
    $forall m, n : ZZ . ("coprime"(m, n) arrow.r.double "coprime"(n, m)).$
  ]
]

#exercise[
  Нехай $k, m : ZZ$ такі, що $k > 0$, $m > 0$ і $k divides m$. Доведіть, що
  $k lt.eq m$. (Вказівка: скористайтеся вправами 14.41 (a) і 14.39 (a).)
]

#exercise[
  Наведіть неформальне доведення леми, вираженої в рядку (7) рисунка 14.23.
]
