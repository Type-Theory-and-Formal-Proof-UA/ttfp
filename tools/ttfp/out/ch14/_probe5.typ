#import "/book.typ": *

Проба: A #(ld)-тексту. B: #(ld)-версія. C: у #(ld)-доведенні. D: #(ld0)-версія.

E: виведення в #(ld).

F: текст #(ld) тут.

G: #ld-версію так не можна.

H: #(ltwo)-версія, #(lp)-вираз, #(lc)-текст.
